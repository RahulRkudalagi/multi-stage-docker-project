apiVersion: v1
kind: Service
metadata:
  name: multi-stage-service
  namespace: course-project
spec:
  type: NodePort
  selector:
    app: multi-stage-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 32000
